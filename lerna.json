{
  "npmClient": "npm",
  "version": "independent",
  "packages": ["packages/*"],
  "ignoreChanges": ["*.md", "*.yml", "**/*.test.*", "**/*.stories.*", "@(!(package)).json"],
  "command": {
    "version": {
      "allowBranch": "master",
      "conventionalCommits": true,
      "message": "chore(release): publish"
    }
  }
}
